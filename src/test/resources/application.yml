# ----------------------------------------
# logging
# ----------------------------------------
logging:
  level:
    root: DEBUG
    web: DEBUG
    org.hibernate:
      SQL: DEBUG
      type.descriptor.sql.BasicBinder: TRACE

# ----------------------------------------
# DataSource
# ----------------------------------------
spring.datasource:
  driver-class-name: org.postgresql.Driver
  url: jdbc:postgresql://localhost:5432/temportal
  username: temportaluser
  password: temportaluser0428
  initialization-mode: always
